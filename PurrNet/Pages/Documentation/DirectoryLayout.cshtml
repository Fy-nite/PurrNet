@page
@{
    ViewData["Title"] = "Directory Layout";
}

<h1>Directory Layout</h1>

<p>Where <code>purr</code> and script-installed packages keep their files.</p>

<pre><code>~/.purr/
└── bin/                      # Binaries installed from release assets
    └── &lt;package-name&gt;

~/.fur/
└── packages/
    └── &lt;package-name&gt;/       # One directory per script-installed package
        ├── .git/             # Full git clone of the package's repository
        ├── furconfig.json    # Metadata snapshot written at install time
        └── &lt;installer&gt;       # e.g. install.sh / uninstall.sh
</code></pre>

<p><strong>Notes</strong></p>
<ul>
    <li>Release-asset-installed executables are placed in <code>~/.purr/bin</code> (or <code>%USERPROFILE%\.purr\bin</code> on Windows).</li>
    <li>Script-based packages are full git clones under <code>~/.fur/packages</code> so installers can run from the repo.</li>
    <li><code>purr</code> does not automatically remove binaries from <code>~/.purr/bin</code> when uninstalling script-based packages; remove them manually if needed.</li>
</ul>
